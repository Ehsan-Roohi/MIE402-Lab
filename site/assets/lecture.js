(()=>{const slides=[...document.querySelectorAll('.lecture-slide')],mode=document.querySelector('#mode'),prev=document.querySelector('#previous'),next=document.querySelector('#next'),status=document.querySelector('#status'),keys=document.querySelector('#keys');let active=false,index=0;const hash=location.hash.match(/^#slide-(\d+)$/);if(hash)index=Math.max(0,Math.min(slides.length-1,Number(hash[1])-1));function show(){slides.forEach((s,i)=>s.classList.toggle('current',i===index));prev.disabled=index===0;next.disabled=index===slides.length-1;status.textContent=active?`${index+1} / ${slides.length}`:'';if(active)history.replaceState(null,'',`#slide-${index+1}`);}function toggle(v){active=v;document.body.classList.toggle('presenting',active);mode.textContent=active?'Reading view':'Presentation view';mode.setAttribute('aria-pressed',String(active));[prev,next,keys].forEach(x=>x.hidden=!active);show();if(active)window.scrollTo(0,0);else slides[index].scrollIntoView();}mode.addEventListener('click',()=>toggle(!active));function move(n){index=Math.max(0,Math.min(slides.length-1,index+n));show();window.scrollTo(0,0);}prev.addEventListener('click',()=>move(-1));next.addEventListener('click',()=>move(1));document.addEventListener('keydown',e=>{if(!active||e.altKey||e.ctrlKey||e.metaKey||['INPUT','TEXTAREA','SELECT'].includes(document.activeElement.tagName))return;if(e.key===' '&&document.activeElement.tagName==='BUTTON')return;if(['ArrowRight','PageDown',' '].includes(e.key)){e.preventDefault();move(1)}if(['ArrowLeft','PageUp'].includes(e.key)){e.preventDefault();move(-1)}if(e.key==='Escape')toggle(false);if(e.key==='Home'){index=0;show()}if(e.key==='End'){index=slides.length-1;show()}});show()})();